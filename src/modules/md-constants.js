const IMPORT_LIMIT = 100;
const AUTO_CLOSE_ERROR = 7000;
const AUTO_CLOSE = 5000;
const POPPER_DELAY = 100;

const STATIC_PICKER_OPTS = {
	noOptionsText: ".",
	loadingText: "...",
};

const Constants = {
	IMPORT_LIMIT,
	AUTO_CLOSE_ERROR,
	AUTO_CLOSE,
	POPPER_DELAY,
	STATIC_PICKER_OPTS,
};

export default Constants;
