const SCOPES = Object.freeze({
	USER: "user",
	DEPT: "dept",
	COMP: "comp",
	APP: "app",
});

const KEYS = Object.freeze({
	CMS_GROUP_ALIAS: "cms-group-alias",
});

const UserSettings = {
	SCOPES,
	KEYS,
};

export default UserSettings;
