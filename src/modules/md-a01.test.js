import { it } from "vitest";
import A01 from "./md-a01";
import { expect } from "vitest";

it("should paramsToJsonData", () => {
	// const source = { qs: "110", bc: "1" };
	// const expected = [
	// { ShowName: "產品名稱", OpCode: "LIKE", CondData: "%110%" },
	// 	{
	// 		ShowName: "原印條碼",
	// 		OpCode: "LIKE",
	// 		CondData: `1%`,
	// 	},
	// ];
	// expect(A01.paramsToJsonData(source)).toStrictEqual(expected);
});
